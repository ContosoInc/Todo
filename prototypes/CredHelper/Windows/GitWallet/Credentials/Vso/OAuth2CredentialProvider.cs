namespace GitWallet.Credentials.Vso
{
    /// <summary>
    /// Implements a credential provider for VSO's OAuth2.
    /// </summary>
    internal class OAuth2CredentialProvider : CredentialProvider
    {
    }
}
