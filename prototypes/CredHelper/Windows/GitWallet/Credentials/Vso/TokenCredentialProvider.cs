namespace GitWallet.Credentials.Vso
{
    /// <summary>
    /// Implements a credential provider for VSO's personal tokens.
    /// </summary>
    internal class TokenCredentialProvider : CredentialProvider
    {
    }
}
